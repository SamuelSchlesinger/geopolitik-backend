_common: !include "../shared.yaml"

name: geopolitik-server
<<: *common

dependencies:
  - base

library:
  source-dirs:
    - src
  dependencies:
    - base
  <<: *library

executables:
  exe:
    main: Main.hs
    source-dirs: app
    dependencies:
      - geopolitik-server
    <<: *executable

tests:
  test:
    main: Test.hs
    source-dirs: test
    dependencies:
      - geopolitik-server
    <<: *test
